<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ================================================================
         Venezuela360: Vistas — Retención Municipal (IAE)
    ================================================================ -->
    <record id="view_account_wh_municipal_list" model="ir.ui.view">
        <field name="name">account.wh.municipal.list</field>
        <field name="model">account.wh.municipal</field>
        <field name="arch" type="xml">
            <list string="Retenciones Municipales"
                  decoration-success="state == 'posted'"
                  decoration-muted="state == 'cancel'">
                <field name="name"/>
                <field name="date"/>
                <field name="partner_id"/>
                <field name="municipality_id"/>
                <field name="state_ve_id" string="Estado"/>
                <field name="rate_pct" string="Alícuota (%)"/>
                <field name="amount_bs" string="Retenido (Bs)"
                       widget="monetary" options="{'currency_field': 'currency_bs_id'}"/>
                <field name="amount_usd" string="Retenido (USD)"/>
                <field name="state" widget="badge"/>
                <field name="currency_bs_id" invisible="1"/>
            </list>
        </field>
    </record>

    <record id="view_account_wh_municipal_form" model="ir.ui.view">
        <field name="name">account.wh.municipal.form</field>
        <field name="model">account.wh.municipal</field>
        <field name="arch" type="xml">
            <form string="Comprobante Retención Municipal">
                <header>
                    <button name="action_confirm" string="Confirmar" type="object"
                            class="btn-primary"
                            attrs="{'invisible': [('state', '!=', 'draft')]}"/>
                    <button name="action_post" string="Contabilizar" type="object"
                            class="btn-primary"
                            attrs="{'invisible': [('state', '!=', 'confirmed')]}"/>
                    <button name="action_cancel" string="Cancelar" type="object"
                            attrs="{'invisible': [('state', 'in', ['draft','cancel'])]}"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,confirmed,posted"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name" readonly="1"/></h1>
                    </div>
                    <group>
                        <group string="General">
                            <field name="move_id"   attrs="{'readonly': [('state', '!=', 'draft')]}"/>
                            <field name="partner_id" readonly="1"/>
                            <field name="date"       attrs="{'readonly': [('state', '!=', 'draft')]}"/>
                        </group>
                        <group string="Municipio y Actividad">
                            <field name="municipality_id" attrs="{'readonly': [('state', '!=', 'draft')]}"/>
                            <field name="state_ve_id" readonly="1"/>
                            <field name="economic_activity"/>
                            <field name="rate_pct" string="Alícuota IAE (%)"
                                   attrs="{'readonly': [('state', '!=', 'draft')]}"/>
                        </group>
                    </group>
                    <group string="Cálculo">
                        <field name="taxable_amount_bs" readonly="1"
                               widget="monetary" options="{'currency_field': 'currency_bs_id'}"/>
                        <field name="amount_bs" readonly="1" class="fw-bold text-success"
                               widget="monetary" options="{'currency_field': 'currency_bs_id'}"/>
                        <field name="amount_usd" readonly="1"/>
                        <field name="rate" readonly="1" string="Tasa BCV (Bs/USD)"/>
                    </group>
                    <group string="Asiento">
                        <field name="journal_entry_id" readonly="1"/>
                    </group>
                    <field name="currency_bs_id" invisible="1"/>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="action_account_wh_municipal" model="ir.actions.act_window">
        <field name="name">Retenciones Municipales (IAE)</field>
        <field name="res_model">account.wh.municipal</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
